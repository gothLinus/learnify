<%= form_with model: @collection, local: true do |f| %>
  <label for="name">Name</label>
  <%= f.text_field :name, placeholder: "Name" %>

  <% if @collection.errors[:name].present? %>
    <p><%= @collection.errors[:name].first %></p>
  <% end %>

  <%= f.submit "Save" %>
<% end %>
